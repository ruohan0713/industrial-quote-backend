# 环境配置
NODE_ENV=production
PORT=3000

# Supabase配置
# 从 https://app.supabase.com/project/YOUR_PROJECT/settings/api 获取
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# JWT密钥（用于小程序会话，生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"）
JWT_SECRET=your-random-jwt-secret-key-here

# 微信小程序配置
# 从微信小程序后台获取
WECHAT_APPID=your-wechat-appid
WECHAT_SECRET=your-wechat-secret

# 文件上传配置
UPLOAD_MAX_SIZE=10485760
UPLOAD_DIR=./uploads

# 数据库配置（如果使用MySQL而非Supabase）
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=your-password
# DB_NAME=quote_platform

